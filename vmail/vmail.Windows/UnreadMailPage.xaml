<Page
    x:Class="vmail.UnreadMailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:vmail"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock VerticalAlignment="Center"  Margin="10,10,10,10" HorizontalAlignment="Center" FontSize="48">Unread Mail</TextBlock>
        <ListView Grid.Row="1" Name="mailList" Margin="10,10,10,10" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Tapped="StackPanel_Tapped">
                        <TextBlock Name="txtSubject" Text="{Binding subject}" Margin="10,5" VerticalAlignment="Center"/>
                        <TextBlock Name="txtFrom" Text="{Binding from}" Margin="10,5" VerticalAlignment="Center"/>
                        <Button Name="btnRead" HorizontalAlignment="Right">Read</Button>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
